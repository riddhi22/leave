<div>
<br>
	<table>
<thead>
    <tr>
<!--      <th>oid</th>  -->
      <th scope="col">From Person</th>
      <th scope="col">From Date</th>
      <th scope="col">To Date</th>
      <th scope="col">Status</th>
      <th scope="col">Changes</th>
       <th scope="col">Reason</th>
    </tr>
  </thead>
  <tbody>
    {{#each applis}}
	<tr>
 <!-- <td>{{this._id}}  -->
		<td>{{this.fromPerson}}</td>
		<td>{{this.from}}</td>
		<td>{{this.to}}</td>
		<td width="150%">

			<button class="btn btn-info">Accept</button> <button class="btn btn-info">Reject</button>
			<form method="POST">
			<input type="hidden" name="ouid" value={{this._id}}>
			<button class="btn btn-success" type="submit" action="/applicationchange/accept">Accept</button> 
			<button class="btn btn-danger" type="submit" action="/applicationchange/reject">Reject</button>
			<button class="btn btn-warning" type="submit" style="margin-top:5px" formaction="/users/application/reqchang">Request Changes</button>
			</form> 
		</td>

		<td>{{this.reason}}</td>
	</tr>	
	{{/each}}
  </tbody>
</table>
</div>


